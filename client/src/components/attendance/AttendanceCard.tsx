import { Card, CardContent, CardHeader } from "@/components/ui/card";

export const AttendanceCard = () => {
  return (
    <Card>
      <CardHeader>{/* Insert logo here */}</CardHeader>
      <CardContent>
        <p>Card Content</p>
      </CardContent>
    </Card>
  );
};
